{grasp} = require '../_helpers'
{strict-equal: equal} = require 'assert'

suite 'runner: search' ->
  suite 'class Runner' ->
    suite 'search-targets' ->

    suite 'do-search' ->
      test '@target-paths' ->

    suite 'prepare-search' ->
      test '@search' ->

    suite 'search-input' ->
      test '@results-format' ->
