{grasp} = require '../_helpers'
{strict-equal: equal} = require 'assert'

suite 'replacer' ->
  suite 'parse-filter-obj' ->

  suite 'one filter' ->
    test 'no actions' ->
    test 'no matching actions' ->
    test 'matching action' ->

  suite '2 filters' ->
    test 'one matching action' ->

    test 'two matching actions' ->

  suite 'get-filters' ->

  suite 'get-raw' ->


