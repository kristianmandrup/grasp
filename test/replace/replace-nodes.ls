{grasp} = require '../_helpers'
{strict-equal: equal} = require 'assert'

suite 'replace-nodes' ->
  suite 'class ReplaceNodes' ->
    test 'create instance' ->

    test 'iterate (nodes)' ->

    test 'process (node)' ->